dependencies:
  pre:
    - curl https://install.meteor.com | /bin/sh
    - sudo npm install -g laika
test:
  pre:
    - mongod
  override:
    - laika
deployment:
  production:
    branch: production
    commands:
      - meteor deploy www.roborally.com
  staging:
    branch: master
    commands:
      - meteor deploy staging.roborally.com
